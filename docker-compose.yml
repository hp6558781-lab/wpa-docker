version: '3.8'\n\nservices:\n  app:\n    image: your_app_image:latest\n    ports:\n      - "8080:80"  # Host port: Container port\n    environment:\n      - ENV_VAR1=value1\n      - ENV_VAR2=value2\n    volumes:\n      - app_data:/var/lib/app/data  # Mounting volume\n    deploy:\n      resources:\n        limits:\n          cpus: '0.50'  # Limit to 0.5 CPU\n          memory: 512M  # Limit to 512MB RAM\n\nvolumes:\n  app_data: